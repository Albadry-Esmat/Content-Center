# Campaign Scope Batch 4 browser smoke

Reviewed `/generator?demo=1` on the Batch 4 working tree on 2026-08-18.

The public demo loaded in local-first mode without an account. The compose panel displayed the `AI FOUNDATION DRAFT` card with `Build the grounding plan.`, the explicit `Generate foundation draft` action, `Local provider · stays on this device`, and the message that nothing is sent until the user chooses the action. The existing Campaign Scope summary and grouped sections remained visible, including simple CapCut and DaVinci defaults and the 1 long-form + 2 before + 5 after campaign shape.

No generation action was triggered during this browser review. The visible action remains explicit and the demo continued to state that no provider request is made until the user chooses to generate.
